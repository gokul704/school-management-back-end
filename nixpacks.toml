# Railway Nixpacks configuration for Node.js backend
[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "npm start"

